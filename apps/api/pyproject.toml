[tool.ruff]
line-length = 100
target-version = 'py312'
extend-exclude = ['.venv', '__pycache__']

[tool.ruff.lint]
select = ['E', 'F', 'I', 'B', 'UP', 'ASYNC']

[tool.mypy]
python_version = '3.12'
strict = true
warn_unused_configs = true
