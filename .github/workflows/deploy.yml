name: Deploy

on:
  workflow_dispatch:
  push:
    tags: ['v*.*.*']

jobs:
  release:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Production gate
        run: echo 'Production deployment is gated until hosting credentials are configured.'
